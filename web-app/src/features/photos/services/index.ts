// Server-side service (only for API routes)

// Client-side service (for client components)
export { photoClientService } from "./photo-client-service";
export { photoService } from "./photo-service";
