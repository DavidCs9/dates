{
  "name": "coffee-date-chronicles",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-app"
  ],
  "scripts": {
    "prepare": "husky",
    "build": "cd web-app && npm run build",
    "lint": "cd web-app && npm run lint",
    "lint:precommit": "cd web-app && npm run lint:precommit",
    "format": "cd web-app && npm run format"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}
